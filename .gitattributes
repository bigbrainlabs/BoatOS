# Linux-Deploy-Artefakte MÜSSEN LF behalten — ein CRLF-Shebang bricht auf dem Pi
# (#!/bin/bash\r → "bad interpreter"). Wird auch unter Windows editiert/gebaut.
*.sh        text eol=lf
*.service   text eol=lf
*.conf      text eol=lf
firstrun.sh text eol=lf
